version: 2

models:
  - name: mart_top_tracks
    description: "Latest run’s top-100 tracks with artist context."
    columns:
      - name: track_id
        tests: [not_null, unique]
  - name: mart_artist_summary
    description: "Per-artist KPIs aggregated from intermediate fact."

